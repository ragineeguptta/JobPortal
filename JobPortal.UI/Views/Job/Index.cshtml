@model List<JobPortal.UI.ViewModels.Job.JobListViewModel>

<h2>Job Listings</h2>

@foreach (var job in Model)
{
    <div>
        <h3>@job.Title</h3>
        <p>@job.Company - @job.Location</p>
    </div>
}